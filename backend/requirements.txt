# FastAPI backend (Python 3.13) — Atlas
# Pinned to match the versions already provisioned in .venv so they stay
# compatible with the rest of the stack (supabase client, docling, langchain).
fastapi==0.139.2
uvicorn[standard]==0.51.0
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.14.2

# Test
pytest==8.3.4

# PDF ingestion (Docling) + FastAPI multipart uploads for document ingest.
python-multipart>=0.0.9
